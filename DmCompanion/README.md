# DM Companion

## Supported platforms

- Android
- web (recommended)

There are build files for other platforms in this project (iOS, Windows, MacOS, Linux). The project has not been tested on these platforms.

## Commands

To install dependencies, run:

```bash
flutter pub get
```

To re-generate generated files, run:

```bash
dart run build_runner build
```
